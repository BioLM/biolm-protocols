name: E1 600M Predict from Sequences
description: Score a list of protein sequences with E1 600M for log-probability in batches.

example_inputs:
  sequences: ["MKFLKFSLLTAVLLSVVFAFSSCGDDDD", "GLPRKILCAI"]
  batch_size: 8
  deduplication: false

inputs:
  sequences:
    type: list_of_str
    label: Protein sequences
    required: true
    max_length: 10000
    help_text: List of protein sequences to score with E1 600M
  batch_size:
    type: integer
    label: Batch Size
    optional: true
    advanced: true
    initial: '8'
    min: 1
    max: 8
    help_text: Number of sequences per batch
  deduplication:
    type: boolean
    label: Deduplicate output
    optional: true
    initial: false
    help_text: Remove duplicate records (by sequence) from results

progress:
  total_expected: ${{ len(sequences) }}

ranking:
  field: log_prob
  order: descending
  top_n: 10

writing:
  deduplicate: ${{ deduplication }}

tasks:
  - id: sequences_batch
    type: gather
    from: sequences
    fields: [sequence]
    into: ${{ batch_size }}
    skip_if_empty: true

  - id: e1_predict
    app: e1-600m
    class: E1Model
    method: predict_log_prob
    depends_on: [sequences_batch]
    foreach: ${{ sequences_batch.results }}
    skip_if_empty: true
    request_body:
      items: ${{ item }}
    response_mapping:
      log_prob: ${{ response.results[*].log_prob }}
    fail_on_error: true
